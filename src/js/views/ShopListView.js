import { elements } from './base';

// TODO: Create markup
// Create scss

const createListItem = (item) => {
    return `
        //TODO: Add html
    `;
}

export const renderShopListItem = (item) => {
    //get the parent shoppingList <ul>
    //insertAdjasentHTML('afterbegin', item);
}